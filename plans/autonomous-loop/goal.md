# Autonomous Phase-Development Goal

Progress the AI Career Learning Platform through its approved roadmap, one
independently verifiable slice at a time. Preserve constitutional gates, record
only verified evidence, and keep the controller resumable without conversation
history.

## Current lanes

The validation lane remains `V00 - Candidate Role Evidence`,
`WAITING_EXTERNAL / Revise`; V01 remains locked. Missing practitioner,
recruitment, demand, and measured cost evidence is an external dependency, not a
pause for user review. This lane alone controls role approval, role-specific
content, learning logic, and readiness claims.

Foundation phases F00-F02 are `PASSED / Continue`. F02 implementation revision
`16bf84c9919ccf16d1e06e1542245737bd61cde4` passed run `29510082305`, and its
controller acceptance revision `8963101805d6f29f4701c91764b5563f07ff07c8`
passed exact run `29511515229`.
`F03 - Portable Container Runtime and Non-Production Preview Baseline` is
defined but not started. Its approved order is API OCI artifact,
web/private-binding topology, then one exact-revision non-production preview.

## Standing controller delegation

The controller writes only on `automation/v00-phase-loop`. Under the user's
standing delegation of 2026-07-15, Codex owns selection and acceptance of the
next eligible technical or controller action and may stage, commit, and push one
bounded slice without waiting for user review. Exact GitHub Actions is the
publication and acceptance gate.

That delegation cannot fabricate practitioner confirmation, recruitment-channel
proof, observed demand, measured cost, or constitutionally required human
approval. It cannot unlock V01 without V00, broaden a phase, include unrelated
working-tree changes, publish secrets, or substitute deployment for validation.

## Deployment boundary

GitHub remains the source, CI, and exact-revision acceptance control plane. The
user reports connecting the repository to Vercel. Official documentation
inspected on 2026-07-16 describes Vercel Services, OCI container deployment, and
private service bindings; those Beta capabilities can be evaluated as a
replaceable preview adapter while preserving the approved portable-container
boundary.

No local `.vercel` link, `vercel.json`, Vercel CLI, or deployment is verified.
A branch push may nevertheless trigger an automatic preview through the
pre-existing Git integration. It remains unverified, is not intentionally
invoked here, and cannot count as F03 evidence or advance F03-03.
F03 therefore permits only a later ephemeral preview after the API and web
container slices pass. It does not approve Vercel for production, staging,
public API ingress, persistence, credentials, product maturity, V00/V01, or
V16A/V16B/V17A evidence.

## Exact next action

After the definition amendment passes exact GitHub Actions, stop. On a later
invocation, revalidate F03 and implement only
`F03-01 - FastAPI portable OCI artifact`; do not add web/Vercel topology,
publish an image, or intentionally invoke deployment in that slice. An
integration-triggered preview remains unverified and is not F03 evidence.
