# Autonomous Phase-Development Goal

Progress the AI Career Learning Platform through its approved roadmap, one
independently verifiable phase or bounded repair at a time. Preserve
constitutional gates, record only verified evidence, and keep the controller
resumable without conversation history.

## Current lanes

The validation lane remains `V00 - Candidate Role Evidence`,
`WAITING_EXTERNAL / Revise`; V01 remains locked. Missing practitioner,
recruitment, demand, and measured-cost evidence is external and cannot be
fabricated or inferred from foundation work.

Foundation phases F00-F03 are durably `PASSED / Continue`. F04 is the active
foundation phase and is `FAILED_RETRYABLE / Revise`. Starting head
`ca08301b46e49945a805f20a07866a931a8e81e0` is mergeable, passed exact GitHub
Actions run `29616557723`, and has a GitHub-linked `READY` Vercel preview, but
F04's exit evidence is incomplete or invalid.

## Standing controller delegation

The controller accepts automation branches matching `automation/**`; pull
request #5 is F04's integration and evidence surface. Codex owns reversible
technical decisions, implementation, repair, commit, push, CI repair, and phase
acceptance without chat approval. Every technical phase still requires an
independently reviewed implementation revision whose exact required checks pass,
then a separate acceptance-state revision whose own exact checks pass.

Pending, failed, skipped, cancelled, stale, merge-ref-only, previous-head, or
chat-only evidence cannot accept a phase. Vercel evidence must also identify the
exact deployment and Git SHA required by the phase contract.

## F04 repair boundary

F04 remains web-only and role-neutral. The current repair program must correct:

- roadmap-derived active and future foundation projection;
- truthful rollback or supported preview-alias reversion evidence;
- automated deployment-protection-aware rendered-page verification;
- reproducible Vercel project, Node, and npm configuration;
- required build, artifact, cache, latency, runtime, and cost measurements; and
- separately attributable removal of the unrelated tracked archive if confirmed.

F04 adds no FastAPI deployment, PaaS vendor, database, learner or role state,
curriculum, assessment, mastery, readiness, LLM access, beta traffic, production
promotion, or V00/V01 evidence.

## Exact next action

After the bounded state/controller reconciliation revision passes its exact
checks, stop. The only next eligible action is:

```text
Demonstrate a supported non-production Vercel alias reversion between two exact
preview revisions, verify the served SHA after each switch, and restore the
intended final alias target.
```

Do not define F05, merge pull request #5, or close issue #3 until every F04 exit
condition is satisfied by an implementation revision and separate
acceptance-state revision.
