# Autonomous Phase-Development Goal

Progress the AI Career Learning Platform through its approved roadmap, one
independently verifiable phase at a time. Preserve constitutional gates, record
only verified evidence, and keep the controller resumable without conversation
history.

## Current lanes

The validation lane remains `V00 - Candidate Role Evidence`,
`WAITING_EXTERNAL / Revise`; V01 remains locked. Missing practitioner,
recruitment, demand, and measured-cost evidence is an external dependency, not a
pause for user review. This lane alone controls role approval, role-specific
content, learning logic, and readiness claims.

Foundation phases F00-F02 are `PASSED / Continue`. F02 controller revision
`8963101805d6f29f4701c91764b5563f07ff07c8` passed exact run `29511515229`.
The current phase is `F03 - GitHub Phase-Gate Control Plane` from GitHub issue
#2. The preceding definition revision `5b19455034583da0d0ec40c82edf70423b93b2df`
has only the three legacy checks and is entry evidence, not F03 acceptance.

## Standing controller delegation

The controller writes only on `automation/v00-phase-loop` and uses pull request
#1 as its persistent integration and evidence surface. Codex owns selection,
implementation, repair, commit, push, and acceptance of internally decidable
technical phases without waiting for user review. Exact GitHub Actions on the
checked-out revision is the acceptance authority.

Each technical phase requires an independently reviewed implementation revision
whose exact five checks pass, followed by a separate acceptance-state revision
whose own exact five checks pass. Pending, failed, skipped, cancelled, stale,
merge-ref-only, or previous-head checks cannot accept a phase.

That delegation cannot fabricate practitioner confirmation, recruitment-channel
proof, observed demand, measured cost, or constitutionally required external
approval. It cannot unlock V01 without V00, broaden a phase, include unrelated
working-tree changes, publish secrets, or substitute technical foundation work
for validation evidence.

## F03 boundary

F03 adds only a versioned repository policy, deterministic fail-closed validator,
exact-head CI contract, deterministic lane projection, adversarial tests, and
the implementation/acceptance evidence loop. It adds no database, persistence,
authentication, tenancy, role or learner model, competency graph, curriculum,
assessment, mastery, simulation, readiness, LLM call, deployment configuration,
Vercel configuration, or product claim.

## Later deployment boundary

Vercel deployment is not F03. It remains a possible separately gated F04 that
must reconcile Vercel with the managed-PaaS technical constitution before any
deployment configuration is added. A pre-existing Git integration may create an
unverified provider side effect on push; that side effect is not F03 evidence
and does not advance any phase.

## Exact next action

Complete issue #2 through its implementation revision, separate acceptance-state
revision, and both exact five-check runs. Then stop before F04. The next eligible
action after F03 is:

```text
Formally define and implement F04 — Vercel deployment baseline from GitHub issue #3.
```
